import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.servomaster.witblox',
  appName: 'Servo Master',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
